import Component from "$store/components/WinnersSection.tsx";
import type { Props } from "$store/components/WinnersSection.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
