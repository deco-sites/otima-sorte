import Component from "$store/components/SwiperShelf.tsx";
import type { Props } from "$store/components/SwiperShelf.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
