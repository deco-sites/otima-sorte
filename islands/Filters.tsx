import Component from "$store/components/search/Filters.tsx";
import type { Props } from "$store/components/search/Filters.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
