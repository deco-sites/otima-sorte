import Component from "$store/components/header/MenuMobile.tsx";
import type { Props } from "$store/components/header/MenuMobile.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
